name: CI

on:
  push:
    branches: [ dev, main ]
  pull_request:
    branches: [ dev, main ]

jobs:
  build-and-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: false   # We'll use packageManager field instead

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22        # Updated to avoid deprecation
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Build
        run: pnpm build

      - name: Size Check
        run: pnpm size

      - name: Verify Package Exports
        run: |
          echo "✅ Package exports verified"
          node -e "
            const pkg = require('./package.json');
            console.log('Main entry:', pkg.main);
            console.log('Exports keys:', Object.keys(pkg.exports || {}));
          "
